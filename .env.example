# Application Settings
NODE_ENV=development  # Set to 'production' on Render
PORT=5000             # Render uses port 10000 by default

# Database Configuration (PostgreSQL)
# For Render deployment, use the Internal Database URL provided in the Render dashboard
DATABASE_URL=postgres://username:password@host:port/database
PGUSER=username
PGPASSWORD=password
PGHOST=host
PGPORT=5432
PGDATABASE=database
PGSSL=true            # Required for Render PostgreSQL

# File Storage
UPLOAD_DIR=/var/data/uploads  # Location for image storage on Render

# Session Configuration
SESSION_SECRET=your-secure-session-secret

# Email Integration (SendGrid)
SENDGRID_API_KEY=your-sendgrid-api-key
SENDGRID_FROM_EMAIL=no-reply@rpm-auto.com
ADMIN_EMAIL=admin@rpm-auto.com

# Admin Credentials (Development Only)
# In production, set these via the Render dashboard
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-admin-password