# Database Configuration
# For local development
DATABASE_URL=postgres://username:password@localhost:5432/rpm_auto
# For Render deployment, this will be provided by Render
# DATABASE_URL=postgres://rpm_auto_user:password@postgres-instance.render.com/rpm_auto

# Server Configuration
PORT=5000                  # For local development
NODE_ENV=development       # Use 'production' for Render deployment

# File Upload Directory
# For local development
UPLOAD_DIR=./public/uploads  
# For Render deployment
# UPLOAD_DIR=/var/data/uploads

# Email Configuration (SendGrid)
SENDGRID_API_KEY=your_sendgrid_api_key
EMAIL_FROM=your_verified_email@example.com
EMAIL_REPLY_TO=reply_to_email@example.com

# Admin User Default Credentials (for initial setup)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=secure_password
ADMIN_EMAIL=admin@example.com

# For AWS S3 Storage (if using Option 2 in IMAGE_STORAGE_GUIDE.md)
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_S3_BUCKET=your_bucket_name
# AWS_REGION=us-east-1

# For Cloudinary Storage (if using Option 3 in IMAGE_STORAGE_GUIDE.md)
# CLOUDINARY_CLOUD_NAME=your_cloud_name
# CLOUDINARY_API_KEY=your_api_key
# CLOUDINARY_API_SECRET=your_api_secret

# For Render.com deployment notes:
# - DATABASE_URL will be auto-populated by Render when using their managed PostgreSQL
# - Set NODE_ENV to 'production'
# - SSL is required for Render PostgreSQL connections
# - For file uploads, use a Render Disk with appropriate mount path